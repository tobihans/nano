# Django settings
DEBUG=true
SECRET_KEY=change-me-in-production
ALLOWED_HOSTS=["localhost","127.0.0.1"]
INTERNAL_IPS=["127.0.0.1"]

# Localization
LANGUAGE_CODE=en-us

# Email settings (defaults work with Mailpit in docker-compose.dev.yml)
EMAIL_HOST=localhost
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_PORT=1025
